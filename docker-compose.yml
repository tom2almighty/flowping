services:
  hub:
    image: ghcr.io/tom2almighty/flowping-hub:latest
    container_name: flowping-hub
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      FLOWPING_LISTEN: ":8080"
      FLOWPING_DATA: /data
    volumes:
      - ./data:/data
